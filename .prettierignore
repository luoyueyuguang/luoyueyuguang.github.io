public
node_modules
.quartz-cache
AGENTS.md
# Markdown articles contain LaTeX underscores that Prettier can reinterpret as emphasis.
content/**/*.md
# Vendored / minified third-party assets (KaTeX CSS, fonts, copy-tex bundle).
quartz/static/katex/
